############
###  01  ###
############

print("text", X, Y, COL)               print("Hello World", 50, 100, 15)
escp + run 
strg + r
cls()                                  cls(15)

alt + left/right                       switches tool
RMB on pixel to get color
spr(sprite, X, Y)
circ(X, Y, RAD, COL)
circfill(X, Y, RAD, COL)
rect(X1, Y1, X2, Y2, COL)
rectfill(X1, Y1, X2, Y2, COL)
save <filename>                        save
reboot
load <filename>
dir                                    ls



##########
### 02 ###
##########

-- write comment
function _init()                       once at startup
 indented text
 content of the function
end                                    end of the function

function _draw()                       mostly 30fps but it can lack
end

function _update()                     exactly 30fps
end

shift + enter                          automatically create end
ctrl + s                               save
variable=value
mynumber=40
mynumber=mynumber+1
circ(64,64,myvar)                      circle that changes size
spr(3,myvar,64)                        sprite that moves to the side



##########
### 03 ###
##########

if you change color then it stays like that for later draws
first _update is called and then _draw is called
if variable<value then
 indented text
end
variable<value
variable>value
variable==value
btn(0) is true when left is pressed
btn(1) is true when right is pressed



##########
### 04 ###
##########

proper variable naming
shipx shipy shipsx shipsy
separate variables for separate entity bullet
bullx bully
btnp() is true only when pressing down
sfx(0) plays sound effect number 00
sound 04 loop 13-18 code to get there from 13-50?