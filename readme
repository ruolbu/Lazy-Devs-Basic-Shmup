############
###  01  ###
############
print("text", X, Y, COL)               print("Hello World", 50, 100, 15)
escp + run 
strg + r
cls()                                  cls(15)

alt + left/right                       switches tool
RMB on pixel to get color
spr(sprite, X, Y)
circ(X, Y, RAD, COL)
circfill(X, Y, RAD, COL)
rect(X1, Y1, X2, Y2, COL)
rectfill(X1, Y1, X2, Y2, COL)
save <filename>                        save
reboot
load <filename>
dir                                    ls
